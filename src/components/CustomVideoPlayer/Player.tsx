import { ReactPlayerProps } from "react-player"

export const Player:React.FC<ReactPlayerProps>=  (
    props:ReactPlayerProps
    ):JSX.Element =>{

 
   // const {url,light} = props;
    return(
        <div>
        </div>
    )

}